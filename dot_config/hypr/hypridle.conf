general {
    lock_cmd = ~/.config/hypr/scripts/lock-now.sh
    before_sleep_cmd = ~/.config/hypr/scripts/lock-now.sh
    after_sleep_cmd = ~/.config/hypr/scripts/dpms-on.sh

    # Wait until hyprlock has actually taken the session lock before suspend.
    inhibit_sleep = 3

    # Keep respecting video/game/browser inhibitors globally.
    ignore_dbus_inhibit = false
    ignore_systemd_inhibit = false
    ignore_wayland_inhibit = false
}

# listener {
#     timeout = 300
#     on-timeout = brightnessctl -s set 10%
#     on-resume = brightnessctl -r
# }

listener {
    timeout = 300
    on-timeout = ~/.config/hypr/scripts/lock-now.sh
}

listener {
    timeout = 330
    on-timeout = ~/.config/hypr/scripts/dpms-off.sh
    on-resume = ~/.config/hypr/scripts/dpms-on.sh
    ignore_inhibit = false
}
